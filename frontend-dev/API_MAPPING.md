GET /api/stats
- total_events → Dashboard StatCard (Total Events)
- unique_ips → Dashboard StatCard (Unique IPs)
- threat_avg → Dashboard StatCard (Threat Average)

GET /api/events
- timestamp → Events table
- source_ip → Events table
- honeypot_type → Events table
- port → Events table
- details → Events table